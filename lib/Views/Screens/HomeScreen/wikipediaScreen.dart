import 'package:flutter/material.dart';

class WikipediaScreen extends StatelessWidget {
  const WikipediaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Plant Wikipedia"),
    );
  }
}
