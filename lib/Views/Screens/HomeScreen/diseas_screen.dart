import 'package:flutter/material.dart';

class DiseasScreen extends StatelessWidget {
  const DiseasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Disease Detection"),
    );
  }
}
